rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to trips collection for anyone
    match /trips/{tripId} {
      allow read, write: if true;
    }
  }
}
